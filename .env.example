# core/config.py Settings tarafından okunan tüm ayar anahtarları.
# Gerçek değerleri girip bu dosyayı ".env" olarak kopyalayın (.env gitignore'dadır).

# Varsayılan: proje kökündeki planner.db (mutlak yol). Değiştirmek istemiyorsanız boş bırakın.
DATABASE_URL=

# SQLAlchemy SQL loglarını konsola bassın mı (dev/debug için).
SQLALCHEMY_ECHO=false

# Google Cloud Console'dan indirilen OAuth client secret dosyasının yolu.
GOOGLE_CREDENTIALS_FILE=credentials.json

# İlk girişten sonra otomatik oluşturulan/yenilenen token dosyasının yolu.
GOOGLE_TOKEN_FILE=token.json

# Google Calendar API scope'ları (JSON liste).
GOOGLE_CALENDAR_SCOPES=["https://www.googleapis.com/auth/calendar.events"]

# Faz 3'te ai_advisor.py tarafından kullanılacak — henüz zorunlu değil.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Boş bırakılırsa giriş ekranı devre dışı kalır (uygulama bugünkü gibi doğrudan açılır).
# Bir parola belirlemek için bcrypt hash'i üretip buraya yapıştırın:
#   python -c "import bcrypt; print(bcrypt.hashpw(b'sifreniz', bcrypt.gensalt()).decode())"
AUTH_PASSWORD_HASH=
